# Current Handoff

Local Security Auditor 1.0.4 patch preparation began from `2afc22f` on `main`. Version and release documentation were updated; no scanner production behavior was changed. The 1.0.4 source, wheel, sdist, and real-target OSV-enabled scan are verified locally. The release decision is READY TO TAG v1.0.4. Do not tag, push, or publish as part of this handoff.

The Python 3.12.11 full offline regression passed **247 tests, 241 passed, 0 failed, 6 skipped**. Two offline builds had identical filenames, members, sizes, and SHA-256 values. Wheel: `local_security_auditor-1.0.4-py3-none-any.whl`, 187,855 bytes, SHA-256 `252e28216f054e57a3c31c0b017a4114a3163948f6a9950f45f97e8ede4c99d5`. Sdist: `local_security_auditor-1.0.4.tar.gz`, 138,121 bytes, SHA-256 `95f5f2956fa5060dea49e52041deeae779cb62c105e425d0c3636a38bd1c2cd2`. Archive safety inspection passed. Fresh repo-external core-wheel, `[gemini]` wheel, and sdist installs passed. Twenty installed golden tests, four Gate tests, four additional AI tests, and one fake-provider AI partial-state check passed, with JSON 1.1, SARIF 2.1.0, zh-TW HTML/CSP, source/wheel stable-field equality, and zero Gemini requests in offline mode.

The clean-installed wheel scanned the Trading Platform `deep --offline --no-ai`: 21 machine Findings, 20 groups, 343 assessed exact dependency records, 341 fresh cache hits, zero external requests, deterministic coverage COMPLETE, zero diagnostics, Gate WARN. `.pytest-tmp` existed but did not cause incomplete coverage in this snapshot. After the user separately and explicitly authorized one bounded OSV-enabled check, a fresh installed 1.0.4 wheel ran `deep --osv --no-ai`. `scan.offline=false`; assessed exact 343, unassessed 0, fresh cache 341, stale 0, `NO_DATA` 0, actual OSV batch/detail/total 0, Gemini 0, deterministic coverage COMPLETE, diagnostics 0, Gate WARN. Findings remained 21 machine / 20 groups. Target HEAD matched before and after. Short Git status changed from 11 to 14 entries during the scan and later to 16 during concurrent dashboard work; exact before/after target equivalence is not established. Auditor wrote only an external temporary report and did not intentionally change the target. This concurrent target state is not a scanner regression. The temporary report and environment were cleaned after recording aggregate evidence.

Live OSV/Gemini tests were not enabled. JSON 1.1, SARIF 2.1.0, and Gate policy 1.0 remain unchanged.

Directly nested `exec(compile(...))` is one dynamic-code operation, while standalone compile remains visible. Report dedup requires matching fingerprint and full semantic identity; differing evidence is not silently discarded. The production `behavior.static` / `sast.python` scanner IDs now participate in correlation. Same-location specific command behavior is primary and generic process behavior supporting. Test context is an additive path-shape tag, never a suppression. The dynamic input tag proves only a bounded stored-source AST lookup; it does not assert upstream trust or sandboxing.

Dependency provenance is additive in JSON 1.1: provider OSV, assessed/unassessed exact records, fresh/stale accepted cache keys, cache age range, TTL, and actual network counters. AI requested with zero eligible subjects reports `no_eligible_items` and zero requests; `PATCH_AI_UNAVAILABLE` remains a separate remediation diagnostic. HTML presents source-free test/dynamic context, collapsible supporting signals, provenance, and honest zero-match wording.

Trading Platform deep `--osv --no-ai` source validation: **21 distinct machine Findings** (0 Critical, 0 High, 4 Medium, 1 Low, 16 Info), 20 primary groups. A stable snapshot had deterministic coverage COMPLETE, diagnostics zero, Gate WARN. The prior 22 Finding baseline had two dynamic-code hits at `packages.py:244` from nested exec/compile; the new report retains one outer fingerprint. `test_strategy_runtime.py:243` CMD is primary and PROCESS supporting. Two synthetic credential cases retain Findings with test-context tags. Local CLI path write remains LOW/CWE-22; environment-read INFO signals remain. Dependency inventory 344, exact 343, first-party 1, assessed 343, unassessed 0, 341 fresh cache hits, zero network requests and zero known advisory matches. A later snapshot during target tests had `.pytest-tmp` symlink and parse diagnostics, so overall coverage was PARTIAL and Gate BLOCK. Findings and dependency cache statistics were unchanged. Target Git short status matched immediately before/after each validation scan; Auditor did not execute/import/install or modify target code.

Preserve pre-existing untracked audit reports and `uv.lock`. The local hardening commit contains only source, tests, and documentation. Correlation remains static context rather than exploitability proof; test paths are not safe exemptions; dynamic strategy execution is not sandboxed; zero OSV matches depend on cache freshness and provider knowledge. Do not tag, push, publish, change the target, or start Phase 10 as part of this handoff.

# Current Handoff

Local Security Auditor remains v1 RC `0.9.0` on `main`; Phases 0–9 are complete and Phase 10 has not started. Hardening #6 source changes bound OSV network calls to 10 batch, 50 advisory detail, and 60 total requests by default, with scan-local advisory-ID reuse. A budget hit preserves obtained Findings, leaves unqueried keys incomplete, marks dependency coverage PARTIAL, and blocks the Gate. JSON 1.1 has additive request statistics; zh-TW HTML explains budget exhaustion. See [Dependency Scanner](../docs/DEPENDENCY_SCANNER.md), [Security Gate](../docs/SECURITY_GATE.md), and [Reporting](../docs/REPORTING.md).

The full offline Python 3.12.11 suite passed: `uv run --offline --no-sync python -m unittest discover -s tests -q` — 170 tests, 165 passed, 0 failed, 5 skipped. The one new gated ten-coordinate OSV pilot was attempted against the official endpoint, with at most one batch plus three detail requests. It returned `VULN_PROVIDER_BAD_RESPONSE`; no Finding was produced. The failed harness did not emit actual method counts before its assertion, so do not claim a successful pilot or exact live request count. No second live flow ran. The earlier one-coordinate PyYAML live validation remains verified. See [RC real-world validation](../docs/RC_REAL_WORLD_VALIDATION.md).

The Trading Platform was **not** queried online in hardening #6. Its last offline observation remains 341 unique exact registry keys, 341 `OFFLINE_NO_CACHE`, oversized applicable Secret log, PARTIAL overall coverage, and Gate BLOCK. At batch size 50, a full uncached query would need about seven batches; the new global detail and total ceilings are 50 and 60. Diagnose the ten-coordinate provider response mismatch with bounded metadata-only instrumentation and request fresh authorization before another live flow or any full-project query. Never execute or modify target code.

The ignored 0.9.0 wheel/sdist predate source hardening #4–#6 and must be rebuilt and clean-smoked before external RC validation. Preserve untracked audit reports and `uv.lock`; do not stage them. Do not push, tag, raise Secret limits, exclude logs, or start Phase 10.

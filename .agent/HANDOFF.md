# Current Handoff

No active handoff. Phases 0–9 remain complete and Phase 10 has not started. v1 release hardening #3 prepared the 0.9.0 Release Candidate baseline. The current evidence and remaining limits are in [Project Status](../PROJECT_STATUS.md) and [Release Candidate](../docs/RELEASE_CANDIDATE.md).

`pyproject.toml` is the single version source. `uv build` produced a wheel and sdist; the wheel was installed into a fresh core-only Python 3.12.11 environment outside the checkout. Version/help/import, offline synthetic JSON 1.1, zh-TW HTML, SARIF 2.1.0, Gate PASS, Tk 8.6 import, and a visible installed-GUI main window passed. A second clean environment installed `[gemini]` with SDK 2.25.0; a synthetic high-priority Finding produced `AI_API_KEY_MISSING` with deterministic results retained, and `--offline --ai` produced `AI_OFFLINE`. Neither Gemini nor OSV was contacted in this hardening. Source and installed wheel matched on normalized results and Gate BLOCK for deliberately unsafe synthetic data. The offline source suite ran 149 tests: 145 passed, 0 failed, 4 skipped.

The build archives contained no `.env`, audit reports, caches, temp data, or private path markers. Temporary clean environments, synthetic target, and reports were removed after validation. The ignored `dist/` wheel and sdist remain local and uncommitted. Pre-existing untracked Mosaic reports and `uv.lock` were not staged or removed; `uv run` may have refreshed that untracked lock while syncing the new project version. Confirm final Git state with `git log -1` and `git status`.

Real symlink and UNC behavior, standalone executable/installer, high-DPI and assistive-technology GUI checks, and heuristic redaction remain release limits. The next recommended work is v1 RC real-world validation of medium Python, Node/TypeScript, and mixed-language repositories as static untrusted inputs, before considering 1.0.0.
